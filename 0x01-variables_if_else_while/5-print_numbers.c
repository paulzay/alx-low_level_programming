#include <stdio.h>

/**
 * main - entry point
 *
 * description: tba
 *
 * Return: Always Successful (0)
*/

int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		printf("%d", num);
	}
	putchar('\n');

	return (0);
}
