#include <stdio.h>

/**
 * main - entry point
 *
 * Description: tba
 *
 * Return: Always Successful (0)
*/

int main(void)
{
	char c;

	for (c = 'z'; c >= 'a'; c--)
	{
		putchar(c);
	}

	putchar('\n');

	return (0);
}
