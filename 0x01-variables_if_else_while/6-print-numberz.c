#include <stdio.h>

/**
 * main - entry point
 *
 * Description: tba
 *
 * Return: Always successful (0)
*/

int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		putchar(num + '0');
	}

	putchar('\n');

	return (0);
}
