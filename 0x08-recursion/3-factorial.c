#include "main.h"

/**
 * factorial - fn that returns factorial of number
 * @n: input number
 * Return: type int
*/

int factorial(int n)
{
	if (n == 0)
	{
		return (0);
	}
	else if (n == 1)
	{
		return (1);
	}
	else if (n < 0)
	{
		return (-1);
	}
	else
	{
		return (n * factorial(n - 1));
	}
}
