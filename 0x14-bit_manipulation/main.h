#ifndef _MAIN_H
#define _MAIN_H

#include <string.h>
#include <stddef.h>
#include <stdio.h>

unsigned int binary_to_uint(const char *b);
void print_binary(unsigned long int n);
int _putchar(char c);

#endif
