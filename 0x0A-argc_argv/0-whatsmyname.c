#include <stdio.h>
#include <stdlib.h>

/**
 * main - fn
 * @argc: no of arguments
 * @argv: array
 * Return: 0
*/

int main(int __attribute__((unused)) argc, char *argv[])
{
	int i;

	i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
