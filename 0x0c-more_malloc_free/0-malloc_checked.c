#include "main.h"

/**
 * malloc_checked - fn that allocates memory w malloc
 * @b: int
 * Return: void
*/
void *malloc_checked(unsigned int b)
{
	void *n;

	n = malloc(n);

	if (n == NULL)
		exit(98);
}
